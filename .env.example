# URL publique de l'application (SEO, liens absolus, etc.)
NEXT_PUBLIC_APP_URL=https://tonomi-accessoires.vercel.app

# Compte admin par défaut (fallback si aucun utilisateur en base).
# Ne jamais commiter de vraies credentials de production. En production : utiliser une API auth
# (NextAuth, Supabase, etc.) et ne pas exposer ces variables au client (préférer AUTH_SECRET côté serveur).
# Tests E2E (Playwright) : optionnellement PLAYWRIGHT_ADMIN_EMAIL / PLAYWRIGHT_ADMIN_PASSWORD.
NEXT_PUBLIC_ADMIN_DEFAULT_EMAIL=admin@tonomi.com
NEXT_PUBLIC_ADMIN_DEFAULT_PASSWORD=admin123

# Variables pour une future API auth
# AUTH_SECRET=
# NEXTAUTH_URL=

# Monitoring (Sentry) — optionnel. Voir lib/utils/sentry.ts et @sentry/nextjs.
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_ORG= SENTRY_PROJECT=
